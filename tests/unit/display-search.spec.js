describe("DisplaySearch.vue", () => {
  it("displays emitted status", () => {});

  it("is located correctly within the HomeView page", () => {});

  it("is located correctly within the MyWatchListView page", () => {});
});

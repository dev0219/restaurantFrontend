describe("MyWatchList.vue", () => {
  it('renders search inputs for "Search by list" and "Search by cuisine" for all Restaurants added to the watchlist', () => {});

  it('emits "search" event when "Search" button is clicked', async () => {});

  it("displays an error message for wrong seach inputs", () => {});

  it("renders RestaurantDetailCard for each watchlist Restaurant", () => {});

  it("renders RestaurantDetailCards for filtered by Restaurants added to watch list", () => {});

  it("renders buttons for each watchlist Restaurant", () => {});

  it('emits "edit-Restaurant" event when "Edit" button is clicked', async () => {});

  it('emits "delete-Restaurant" event when "Delete" button is clicked', async () => {});

  it('emits "move-Restaurant" event when "Move" button is clicked', async () => {});
  //
  it("displays move-Restaurant menu with list of available lists and a blank field to create a new list and a save button", () => {});

  it('emits "save-move" event when "Save" button is clicked', async () => {});
  //
  it('displays "View More" button', () => {});

  it('emits "load-more" event when "View More" button is clicked', async () => {});
});

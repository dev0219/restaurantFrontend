describe("SearchBar.vue", () => {
  it("updates the searchQuery data on input change", async () => {});

  it('emits "search" event with the searchQuery on button click', async () => {});

  it('emits "search" event when used in HomePage', async () => {});

  it("is located correctly within the HomeView page", () => {});

  it("is located correctly within the MyWatchList page", () => {});
});

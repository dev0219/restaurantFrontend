describe("Header.vue", () => {
  it("renders buttons for menu items in non-responsive layout", () => {});
  //

  it('toggles "home" button on menu item click', async () => {});

  it('toggles "Dine-in-Restaurants" button on menu item click', async () => {});

  it('toggles "Delivery-Restaurants" button on menu item click', async () => {});

  it('toggles "watchlist" button on menu item click', async () => {});

  it('toggles "sign-up" button on menu item click', async () => {});

  it('toggles "login" button on menu item click', async () => {});
  //

  it("renders a hamburger menu in responsive layout", async () => {});

  it("toggles dropdown menu on hamburger menu click", async () => {});

  it("shows dropdown menu with menu items when hamburger menu is clicked", async () => {});

  it("hides dropdown menu when a dropdown menu item is clicked", async () => {});
  //

  it("displays on top of every page of the app", () => {});
  //
});

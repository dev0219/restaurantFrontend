describe("RestaurantInfo.vue", () => {
  it("displays Restaurant name", () => {});

  it("displays Restaurant image", () => {});

  it("displays cuisine name", () => {});

  it("displays Opened In:", () => {});

  it("displays Restaurant address", () => {});

  it("displays Restaurant phone number", () => {});

  it("displays Restaurant website", () => {});

  it("displays Restaurant rating", () => {});
  //

  it("displays four Restaurant menu items", () => {});
  //

  it("displays availability status (open or closed)", () => {});

  it("displays Delivery or Dine-in option", () => {});

  it('emits "add-to-watchlist" event when "Add To Watchlist" button is clicked', async () => {});

  it('emits "select-Delivery" event when "Delivery Now" button is clicked', async () => {});

  it('emits "select-Dine-in" event when "Book reservations" button is clicked', async () => {});
});

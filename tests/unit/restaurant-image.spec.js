describe("RestaurantImage.vue", () => {
  it("is located correctly within the RestaurantView page", () => {});

  it("is located correctly within the RestaurantInfo page", () => {});

  it("is located correctly within the AddToWatchlist page", () => {});

  it("has a valid image source", () => {});
});

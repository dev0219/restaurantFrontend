describe("SaveBtn.vue", () => {
  it("is located correctly within the SingUpLoginView page", () => {});

  it("is located correctly within the AddToWatchListView page", () => {});

  it("is located correctly within the ReservationView page", () => {});
});
